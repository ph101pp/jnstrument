(function($, THREE, window, document, undefined) {
	var tickable = function(){
/*/////////////////////////////////////////////////////////////////////////////
	Public Properties
/*/////////////////////////////////////////////////////////////////////////////
		this.ticks = 0;
		this.ticker = null;
/*/////////////////////////////////////////////////////////////////////////////
	Public Methods
/*/////////////////////////////////////////////////////////////////////////////
		this.construct = function(){
			console.log("tickable",constructor);

		}
///////////////////////////////////////////////////////////////////////////////	
		this.tack = function(now){
			this.ticks++;
			if(typeof(this.tick) == "function") this.tick(now);
		}

	}
	module.exports = require("../Class.js").abstract(tickable); 
})(jQuery, THREE, window, document)